/**
 * Attack-scene plugin slot for the "drop" work package (issue #32).
 *
 * The package that owns this file replaces its body with an `AttackPlugin`
 * (see web/src/app/scenes/AttackScene.ts) and pushes it onto ATTACK_PLUGINS.
 * Nothing else edits this file, so parallel packages never conflict.
 */
export {};
